spring:
  datasource:
    url: jdbc:h2:mem:booking_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  sql:
    init:
      mode: never

app:
  jwt:
    secret: test-secret-key-for-booking-system-jwt-2026-strong
    expiration-ms: 86400000